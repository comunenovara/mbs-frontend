import { Component, OnInit } from '@angular/core';
import { DynamicDialogConfig, DynamicDialogRef } from 'primeng/dynamicdialog';

@Component({
	selector: 'governative-project-and-procurement-lot-dialog',
	templateUrl: 'governative-project-and-procurement-lot-dialog.component.html'
})

export class EnzoGovernativeProjectAndProcurementLotDialogComponent {
	protected data: any = { };

	constructor(
		public ref: DynamicDialogRef,
		public config: DynamicDialogConfig
	) {
		if(this.config.data !== undefined) {
			this.data = this.config.data;
		}
	}

}